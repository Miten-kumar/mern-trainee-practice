FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5173

# --host is required so vite listens on 0.0.0.0 instead of just
# localhost, otherwise docker's port mapping can't reach it
CMD ["npm", "run", "dev", "--", "--host"]
