import Hero from "../components/Hero"
import ProductList from "../components/productList"

const Home = () => {
    return (
        <main>
            <Hero/>
            <ProductList/>
        </main>
    );
};

export default Home;